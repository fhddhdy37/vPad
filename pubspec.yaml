name: phonepad_flutter
description: PhonePad controller (Flutter port)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  multicast_dns: ^0.3.2+7
  web_socket_channel: ^2.4.0
  vibration: ^1.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true