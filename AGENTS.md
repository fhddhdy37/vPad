# vPad 프로젝트 Codex 지침

본 문서는 **vPad 스마트폰 컨트롤러 시스템** 전용 개발 지침입니다.\
본 프로젝트는 다음 두 구성요소로 이루어집니다.

-   `main.dart` → 스마트폰 컨트롤러 앱 (Flutter)
-   `server.py` → PC 측 컨트롤러 수신 서버 (WebSocket + vgamepad + mDNS)

모든 대답은 한국어로 할 것.

------------------------------------------------------------------------

# 1. 시스템 구조 원칙

## 1.1 아키텍처

\[Flutter App\] → WebSocket(JSON) → \[Python Server\] → vgamepad(XInput)

-   통신 프로토콜: WebSocket
-   데이터 형식: JSON
-   디바이스 에뮬레이션: XInput (ViGEmBus 기반)
-   서버 검색: mDNS (zeroconf)
-   게임 에뮬레이터: Ryujinx

------------------------------------------------------------------------

# 2. 서버 (`server.py`) 개발 규칙

## 2.1 기본 원칙

1.  서버는 완전 비동기(asyncio 기반)로 유지합니다.
2.  클라이언트별로 독립 컨트롤러 인스턴스를 생성합니다.
3.  모든 입력은 신뢰하지 않는 외부 데이터로 간주합니다.
4.  예외 발생 시 서버 전체가 종료되지 않도록 처리합니다.

------------------------------------------------------------------------

## 2.2 컨트롤러 관리 규칙

### 1) 클라이언트별 독립 Gamepad 생성

gp = vg.VX360Gamepad()

-   절대 글로벌 Gamepad 인스턴스를 공유하지 않습니다.
-   WebSocket 연결 단위로 1개 생성합니다.
-   disconnect 시 반드시 reset 처리합니다.

------------------------------------------------------------------------

### 2) 상태 관리 원칙

-   항상 default_state() 기준으로 초기화합니다.
-   watchdog을 통해 일정 시간 입력이 없으면 자동 리셋합니다.
-   float 값은 반드시 clamp 처리합니다.

------------------------------------------------------------------------

### 3) 입력 검증 규칙

-   lx, ly, rx, ry → -1.0 \~ 1.0
-   lt, rt → 0.0 \~ 1.0
-   버튼 → bool 캐스팅

예외 발생 시: - 0 또는 False로 처리 - 서버 종료 금지

------------------------------------------------------------------------

## 2.3 네트워크 규칙

### WebSocket

-   서버 바인딩: 0.0.0.0
-   기본 포트: 8765
-   메시지는 dict 타입만 허용
-   JSON 파싱 실패 시 무시

------------------------------------------------------------------------

### mDNS 광고 규칙

-   Service Type: `_phonepad._tcp.local.`
-   인스턴스명: `PhonePad-{server_name}-{server_id}`
-   TXT 레코드 포함:
    -   name
    -   id
    -   ip
    -   port
    -   os

------------------------------------------------------------------------

## 2.4 멀티 클라이언트 지원 원칙

-   여러 스마트폰 동시 연결 가능
-   각 기기는 독립 컨트롤러로 인식
-   상태 공유 금지
-   Gamepad 객체 공유 금지

------------------------------------------------------------------------

# 3. Flutter 앱 (`main.dart`) 개발 규칙

## 3.1 구조 원칙

1.  UI와 입력 처리 로직 분리
2.  네트워크 송신 로직 분리
3.  입력 상태는 단일 상태 객체로 관리

------------------------------------------------------------------------

## 3.2 입력 전송 규칙

-   전체 상태 객체를 주기적으로 전송
-   서버 watchdog 구조와 정합성 유지
-   패킷 손실 대비

------------------------------------------------------------------------

## 3.3 조이스틱 처리 규칙

-   deadzone 적용
-   -1.0 \~ 1.0 정규화
-   드래그 해제 시 반드시 0 전송

------------------------------------------------------------------------

## 3.4 Long Press 처리

-   ls / rs는 상태 유지 방식으로 구현
-   서버는 상태 기반 처리

------------------------------------------------------------------------

## 3.5 서버 검색

-   mDNS 실패 시 수동 IP 입력 허용
-   UI 스타일 일관성 유지

------------------------------------------------------------------------

# 4. 성능 및 안정성

## 4.1 네트워크 부하 관리

-   60Hz 이상 송신 금지
-   30\~40Hz 권장
-   불필요한 로그 제거

------------------------------------------------------------------------

## 4.2 서버 보호

-   입력 timeout 시 neutral 상태 적용
-   예외 발생 시 연결 단위 종료
-   서버 전체 종료 금지

------------------------------------------------------------------------

# 5. 확장 원칙

-   프로토콜 변경 시 버전 관리
-   JSON 키 변경 시 Flutter/Server 동시 수정
-   XInput 외 확장 시 추상화 레이어 추가

------------------------------------------------------------------------

# 6. 커밋 규칙

feat: 기능 추가\
fix: 버그 수정\
refactor: 구조 개선\
perf: 성능 개선

------------------------------------------------------------------------

# 7. 절대 금지 사항

-   Gamepad 인스턴스 전역 공유
-   예외 미처리 상태에서 서버 종료
-   입력 범위 미검증
-   하드코딩된 IP 의존 구조

------------------------------------------------------------------------

# 8. 목표

-   여러 스마트폰 동시 연결 가능
-   입력 지연 최소화
-   안정적인 XInput 인식
-   네트워크 단절 시 자동 복구
-   유지보수 가능한 코드 구조
